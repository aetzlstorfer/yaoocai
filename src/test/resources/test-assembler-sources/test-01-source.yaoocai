builtin function printInteger(value: integer)            -> printlnInteger

function add(a: integer, b: integer): integer {
  return a + b;
}

function doSomething():boolean {
  return add(1, 2) > 0;
}

function main() {
  var result: boolean = doSomething();
  if(result) {
    printInteger(1);
  }
}